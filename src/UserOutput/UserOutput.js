import React from 'react';

const UserOutput = (props) => {
    return (
        <div>
            <p>Username: {props.name}</p>
            <p>Username: {props.name}</p>
        </div>
    )
};

export default UserOutput;